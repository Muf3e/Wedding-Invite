/**
 * RSVP → GOOGLE SHEET CONNECTION
 *
 * 1. Deploy google-apps-script/Code.gs as a Web app (steps in google-apps-script/README.md).
 * 2. Paste the Web app URL (it ends in /exec) between the quotes below, then save this file.
 *
 * While this is empty the RSVP form tells guests it could not send, so a response is never
 * silently lost. Nothing else needs to change on the website.
 */
window.GOOGLE_SHEET_WEBHOOK_URL = '';
